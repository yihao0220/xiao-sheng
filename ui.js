const UI = {
    showElement: (elementId) => document.getElementById(elementId).style.display = 'block',
    hideElement: (elementId) => document.getElementById(elementId).style.display = 'none',
    updateTaskList: (tasks) => {
        // 实现更新任务列表的逻辑
    },
    showUnfinishedTasks: (tasks) => {
        // 实现显示未完成任务的逻辑
    }
};
